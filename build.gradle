subprojects {

	ext {
		springBootVersion = '2.0.1.RELEASE'
		teamName = 'Retail Reactive'
	}

    description = "reactiveretail-price"
    group = 'com.sapient.retail.service.price'
    version = '0.0.1-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}